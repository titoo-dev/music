// Point d'entrée du seam repository. Depuis la Phase 6, Convex est l'unique
// backend : les repos appellent directement les fonctions Convex (plus de
// sélecteur DATA_BACKEND). Les routes importent chaque repo directement.
export {};
